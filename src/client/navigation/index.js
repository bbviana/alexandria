import * as actions from './actions'

// para o módulo de navegação, faz sentido actionTypes ser dispoinibilizado pela API pública
import * as actionTypes from './actionTypes'

export default {
    actions,
    actionTypes
}